"use client";import{useState}from"react";import{Ban}from"lucide-react";import{voidPurchase}from"./actions";export function VoidPurchaseForm({purchaseId}:{purchaseId:string}){const[open,setOpen]=useState(false);return open?<form action={voidPurchase} className="mt-3 flex gap-2"><input type="hidden" name="purchaseId" value={purchaseId}/><input name="reason" required minLength={3} placeholder="Motivo" autoFocus className="h-9 min-w-0 flex-1 rounded-lg border px-2"/><button className="rounded-lg bg-red-700 px-3 text-xs font-bold text-white">Confirmar</button><button type="button" onClick={()=>setOpen(false)} className="text-xs">Cancelar</button></form>:<button onClick={()=>setOpen(true)} className="mt-2 flex items-center gap-1 text-xs font-bold text-red-700"><Ban size={13}/>Anular recepción</button>}
